<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SecretDiary" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SecretDiary.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.SecretDiary." parent="Base.Theme.SecretDiary" />

    <style name="MyLoginButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@drawable/login_register_button_tint</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:textSize">16sp</item>
    </style>
</resources>